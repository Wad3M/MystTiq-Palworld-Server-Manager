# Apply v0.4.18.2 Changed Files

The Changed Files package is an audit/convenience artifact. For installation, use `MystTiqPalworldServer_v0.4.18.2_FullSource.zip` with `Update-FromDownloads.ps1` so the existing application is closed, the source tree is replaced cleanly, and the complete installed-tree gate runs before relaunch.

Do not overlay this package onto an unknown or older baseline. Its expected baseline is promoted v0.4.17.4.
