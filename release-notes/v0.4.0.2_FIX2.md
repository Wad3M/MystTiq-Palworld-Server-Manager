# MystTiq v0.4.0.2 FIX2 — Console Composition & Warning-Free Build Hotfix

- Corrects the Console composition test to verify the actual Players + Log Tail + Runtime Metrics API wiring.
- Fixes the nullable CS8601 warning in diagnostic restart handling.
- Makes Avalonia desktop compiler warnings promotion-blocking with `TreatWarningsAsErrors`.
- Keeps the product version at v0.4.0.2.
