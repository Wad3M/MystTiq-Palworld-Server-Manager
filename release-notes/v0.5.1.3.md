# v0.5.1.3 — Navigation and UE4SS Parity Correction

This fix preserves the v0.5 shell and restored backend wiring while applying the first ordered screen-by-screen comparison against the supplied v0.2.16.4 GUI.

- Notifications moves from Home to System; the header bell still opens the same persistent notification center.
- The top server strip gains an add control that opens a blank connection profile in Settings and creates a new ID only when saved.
- UE4SS reports the installed runtime version, detection health/evidence, and selectable Palworld/official fork instead of repeating Installed MODs.
- Runtime-version evidence is detected by the headless service and carried through the API DTO; the desktop does not inspect server files.
- Shared single-line TextBox and ComboBox sizing follows the legacy 34-pixel control baseline.
- `GUI_SCREEN_COMPARISON_v0.5.1.3.md` records every legacy destination in order and identifies the remaining parity work.

Promotion requires the complete clean, validation, logic, Windows/Linux build, runtime-smoke, package-entry, and SHA-256 gate.
