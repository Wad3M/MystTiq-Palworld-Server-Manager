# MystTiq Palworld Server Manager v0.3.1.5 FIX1

## Desktop Version Metadata Cleanup

The v0.3.1.5 Doctor implementation passed functional testing, but release validation reported two version-consistency warnings:

- current version missing from `src/PalworldManager/app.manifest`
- stale version metadata in `src/MystTiq.Desktop/MystTiq.Desktop.csproj`

FIX1 synchronizes both files to v0.3.1.5 and adds regression coverage to the v0.3.1.5 logic harness.

The extended Linux lifecycle warning for TLS/auth/non-default bind remains an intentional acceptance skip and is not changed by this hotfix.

No Doctor, GUI, API, TLS, lifecycle, backup, configuration, monitoring, systemd, or PalServer runtime behavior changed.
