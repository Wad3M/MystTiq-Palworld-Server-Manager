# MystTiq Palworld Server Manager v0.2.15.9 FIX1

## Compile Hotfix

This hotfix corrects three release-candidate issues found during Windows validation:

1. `ConfirmedRunning` was referenced but missing from `RuntimeEvidenceState`.
2. The PowerShell regression harness used stale `$LASTEXITCODE` state for `Build.ps1` steps.
3. A stale `v0.2.15.8` source comment caused a version-consistency warning.

No runtime MOD-detection logic, UI behavior, or capability-analysis architecture changed.
