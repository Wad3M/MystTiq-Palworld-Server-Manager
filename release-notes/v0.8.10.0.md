# v0.8.10.0 — Ribbon Icon Originals

- **Your original icons are in.** All 30 Ribbon images now come from your three "originals" packages. They are the
  full framed tiles, and nine of the batch 3 icons are your new designs. Each was resized to 512 px so the app stays
  small, with nothing else changed.
- **Backup and Doctor now have images too.** These buttons appear on every page. Backup uses the Create backup image
  because it does the same thing. Doctor uses the Run Doctor image because it opens the Doctor page.
- **Still without an image:** Refresh (and its page variants), Start, Restart, Stop, Console, Force Stop, Verify
  Files, Install Missing, Run Analysis and Preview Plan. These keep their current icons. The full list is in the
  architecture doc.
