# v0.4.15.0 — World Validator, Recovery & Transaction Center

- Adds active-world structural validation, findings, and local report export.
- Adds remote-safe World Import and Player Recovery ZIP analysis with expiring single-use preview tokens.
- Requires explicit confirmation, a stopped PalServer, and a fresh safety backup for Apply.
- Stages and validates candidate data outside the active save path, atomically swaps it, validates again, and rolls back on failure.
- Persists transaction stages and backup references in server-side JSON journals plus bounded activity/audit summaries.
- Rejects traversal paths, oversized uploads, over-expanded archives, excess entries, missing Level.sav, and noncanonical player recovery files.
- Keeps guild/base binary repair visibly BACKEND REQUIRED until a safe save codec exists.

Promotion gate: strict validation reported 0 errors and 0 warnings; Windows and Linux headless/Avalonia builds succeeded; isolated runtime success and all five failure-stage rollback checks passed; logic gate passed 196/196. Installed-tree gate is repeated by the updater before this package becomes the live baseline.
