# Apply Instructions — v0.4.0.0

Extract the Changed Files ZIP over `C:\GameServers\MystTiqPalLinux`, preserving folders and replacing matching files. Then unblock PowerShell scripts and run the v0.4.0.0 build/test plan.
