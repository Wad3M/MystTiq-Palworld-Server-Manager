# v0.5.1.4 — Legacy Page Detail and Server Tab Correction

This fix applies the supplied v0.2.16.4 page references to the real v0.5 Avalonia shell while preserving the current backend authority and security boundaries.

- Selected server profiles render as contained glass tabs; the add-server control sits directly after the tab list.
- Server Setup restores component/ready/attention summaries, the full environment checklist, supported actions, and operation monitor.
- Configuration removes duplicated lifecycle/status cards and restores server identity, independent Simple/Advanced views, selection markers, slider controls, and default/active values.
- Backup Center exposes Create, Verify Selected, Verify All, Restore, Delete, Refresh, and Open Root at the top.
- Console removes CPU/RAM/thread/recent-metric panels, retains green monospace output, and normalizes timestamps to the first field.
- Workspace restores deployment, health, discovery, and complete location rows with local-profile-only Browse/Open controls and authenticated API save.
- The Windows publisher now closes only artifact-hosted MystTiq processes before publishing, preventing locked-DLL relaunch failures.

Promotion requires the complete clean validation, logic, Windows/Linux build, runtime smoke, archive-entry, and SHA-256 gate.
