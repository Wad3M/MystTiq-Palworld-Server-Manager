# MystTiq Palworld Server Manager v0.2.13.2 FIX1

Corrects a constructor initialization-order defect that caused both installed and portable builds to fail at startup when workspace status was logged before `SessionLogService` existed.

Also adds a unified root `Build.ps1` command for build, packaging, installer, cleanup, version display, and complete release preparation.
