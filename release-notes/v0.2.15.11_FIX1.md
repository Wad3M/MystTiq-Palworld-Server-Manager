# MystTiq Palworld Server Manager v0.2.15.11 FIX1

## MOD Center Extraction Compile Hotfix

The v0.2.15.11 modularization extraction preserved the intended dialog text but four interpolated strings were written with literal physical newlines instead of escaped `\n` sequences. C# therefore reported a cascade of parser errors.

FIX1 restores escaped newlines in:
- MOD verification failure dialog
- verification-report success dialog
- verification-report failure dialog
- compatibility-scan failure dialog

No MOD workflow, coordinator, dashboard-state, runtime evidence, theme, button, or tooltip behavior changed.
