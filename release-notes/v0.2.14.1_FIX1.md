# v0.2.14.1 FIX1 — Coordinated Live Backup

- Detects the specific condition where Palworld keeps an active save file exclusively locked.
- Replaces the terminal live-backup error with an explicit maintenance-backup choice.
- Can save and temporarily stop the server, create and verify the backup, and start the server again.
- Cancelling the prompt leaves the running server untouched.
- Preserves the existing direct backup path when the server is already stopped or live files are readable.
