# MystTiq v0.4.9.0 — Console + RCON Parity & Global Card Containment

- Accepts v0.4.8.0 as the source baseline and fixes its two carried-forward logic-contract mismatches.
- Restores the v0.2.16.4 console filter/search/pause/clear/export workflow around the current combined log tail.
- Adds server-side Source RCON status/doctor/command support through MystTiq API and Core; AdminPassword never crosses to the desktop.
- Labels RCON as legacy/deprecated compatibility functionality while REST/headless APIs remain preferred.
- Adds shared card clipping/text-wrapping containment so dynamic child content cannot visually escape cards.
