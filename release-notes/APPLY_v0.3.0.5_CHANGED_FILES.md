# Apply Instructions — v0.3.0.5

Apply over official Linux/headless baseline **v0.3.0.4 FIX1**.

Preserve repository-relative paths. All deployment/test utilities remain under `scripts`.

After applying, run the v0.3.0.5 Windows gate, then perform the one-time SSH trust setup and passwordless deployment acceptance.
