# v0.4.18.2 — Workspace + Diagnostics + Settings Closeout

This fix corrects the first v0.4.18.0 compile candidate plus inherited v0.4.17.4 logic assertions, and completes the planned pre-v0.5 GUI restoration route.

- Workspace refreshes and saves managed paths through the existing authenticated editable-configuration API, with client syntax validation and rollback-backed headless persistence.
- Browse/open actions are enabled only for the local profile; remote server paths never invoke the GUI computer's filesystem or shell.
- Diagnostics supports copy, text export and a redacted local ZIP support package. Server-side incident artifact collection remains BACKEND REQUIRED.
- Settings retains connection profiles, LAN discovery, process-memory-only bearer tokens, TLS pinning and explicit local bootstrap.
- The final row-level report classifies all 287 historical WPF event handlers.
- Single polling, Windows/Linux parity, secured remote/LAN operation, containment and centered metallic buttons remain preserved.

Promotion requires the complete source gate and updater-driven installed-tree repeat gate.
