# MystTiq Palworld Server Manager v0.3.1.3 FIX1

## Headless Monitoring HttpVersion Compile Hotfix

The v0.3.1.3 desktop targets and monitoring logic checks passed, but the Linux headless publish failed because `HeadlessMonitoringService.cs` references `HttpVersion.Version11` without importing the `System.Net` namespace.

FIX1:

- adds `using System.Net;`
- preserves explicit HTTP/1.1 requests to the local Palworld REST API
- adds regression checks for the required namespace and HTTP/1.1 contract

No API route, monitoring behavior, GUI behavior, lifecycle behavior, TLS behavior, credential handling, or PalServer behavior changed.
