# MystTiq v0.4.0.2 — Avalonia Navigation & Theme Foundation

Rebuilds the shared Avalonia application shell around the v0.2.16.4 navigation model and dark navy/blue visual identity. Existing backend-backed pages remain connected; unfinished destinations are explicit placeholders. The root Build.ps1 release gate now invokes the current version-specific logic/regression harness.
