# v0.4.9.1 — RCON Boundary Gate Correction

v0.4.9.0 compiled successfully for Windows and Linux and passed every runtime-smoke check. Its only failed release assertion treated ordinary explanatory ViewModel text containing the word `socket` as proof that the desktop owned socket code.

v0.4.9.1 corrects that assertion to detect actual transport imports and construction. The required architecture remains enforced: the Avalonia ViewModel and API client contain no RCON network transport, while `MystTiq.Core.Services.PalworldRconService` owns the server-side `TcpClient` implementation. No product behavior or security boundary is weakened.

Promotion gate: strict validation reported 0 errors and 0 warnings; Windows and Linux headless/desktop builds succeeded; Windows runtime smoke passed; logic gate passed 148/148.
