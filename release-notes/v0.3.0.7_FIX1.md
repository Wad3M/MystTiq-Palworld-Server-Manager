# MystTiq Palworld Server Manager v0.3.0.7 FIX1

## Production Doctor Compile & Release-Gate Hotfix

The first v0.3.0.7 Windows gate correctly caught several integration defects before Linux deployment.

### Compile fixes
- corrected `IServerPathProfile` use from nonexistent `ServerExecutablePath` to `ServerExecutable`
- corrected nonexistent `SteamCmdPath` to `SteamCmdExecutable`
- replaced nonexistent `LinuxServerSessionInspector.Inspect()` with the established `LinuxServerLifecycleService.GetStatusAsync()` path

### Release-gate fixes
- corrected the logic harness architecture reference to `production-readiness-v0.3.0.7.md`
- removed stale v0.3.0.6 wording from active HeadlessHost messages
- changed the cleanup helper to construct superseded v0.3.0.6 filenames without tripping the active-version scanner
- documented that PowerShell scripts must be unblocked before invoking the cleanup helper

No accepted v0.3.0.6 runtime behavior was intentionally changed.
