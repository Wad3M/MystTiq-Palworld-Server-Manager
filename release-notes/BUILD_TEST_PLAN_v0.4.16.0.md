# v0.4.16.0 Build and Test Plan

Run Clean, strict Validate, the v0.4.16.0 logic harness with all Windows/Linux builds, and Windows isolated runtime smoke. Verify Activity filtering/export, append-only audit policy, notification persistence, badge/search/severity, read/pin/dismiss/mark-all/self-test/export, mutation audit privacy, remote/LAN security, single polling, containment and relaunch regressions. Package only after the complete gate is clean.
