# v0.8.22.0 — Signing In, Tested From Linux Too

- **Icons on Linux and Windows 10.** The window's minimize, maximize and close buttons, the settings button and the
  tab's close button showed empty boxes on Linux (and on Windows 10). They used an icon font only Windows 11 has; they
  are drawn now, so they show everywhere.
- **More of signing in is tested.** The automated remote sign-in test now also:
  - signs in as an Owner;
  - checks that every Ribbon button the role cannot use is disabled, with the role it needs in its tooltip;
  - runs the Desktop on Linux as well as on Windows.