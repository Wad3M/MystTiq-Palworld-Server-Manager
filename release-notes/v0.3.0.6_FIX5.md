# MystTiq Palworld Server Manager v0.3.0.6 FIX5

## Acceptance Harness Consistency Hotfix

The real remote-management acceptance passed end-to-end, but the PowerShell logic harness still reported two false failures caused by overly literal string matching.

FIX5 corrects only the acceptance contracts:

- enrollment-version validation now checks the stable `0.3.0.6` base-version contract instead of requiring the exact undecorated `VERSION="0.3.0.6"` string
- token-persistence validation now checks the stable `Token persisted:` marker plus the `process memory only` semantic phrase rather than depending on exact spacing/text formatting
- Linux enrollment and Windows LAN acceptance display labels are synchronized to `v0.3.0.6 FIX5`

No runtime code, management API behavior, TLS/authentication policy, systemd behavior, PalServer lifecycle behavior, or Windows WPF behavior changed.

Previous real-world acceptance remains authoritative:

- automated Linux acceptance: 26 passed / 0 failed / 0 warnings
- Windows LAN remote API acceptance: 10 passed / 0 failed
