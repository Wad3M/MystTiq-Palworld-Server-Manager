# MystTiq v0.7.24.0 — Roadmap Doc Audit

## Highlights

- **`docs/roadmap/WINDOWS_BACKPORT_REGISTRY.md`'s watchdog row corrected**: "Service-style watchdog/recovery behavior inspired by Linux/systemd supervision" had sat labeled "Discovery backlog" since v0.6.13.0 actually shipped it (`HeadlessFleetCrashRecoveryService`) — now marked Shipped with a citation.
- **The neighboring log-rotation/retention row was actually investigated**, not assumed: confirmed genuinely still open on both platforms (no size cap, rotation, or retention anywhere in `HeadlessConsoleLogWriter` or elsewhere) — registry entry now records that finding instead of an unverified guess.
- This closes the approved v0.7.20.0–v0.7.24.0 backlog plan.

## Upgrade notes

- Documentation-only release. No code, configuration, or behavior change.

Full detail in [`CHANGELOG.md`](../CHANGELOG.md).
