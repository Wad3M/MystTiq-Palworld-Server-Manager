# Apply Instructions — v0.2.16.3

Apply over validated v0.2.16.2, preserving repository-relative paths, or use FullSource.

Then run Clean / Validate / All and the v0.2.16.3 logic harness.

This revision adds MystTiq self-update awareness and Server Setup badge sizing polish. It does not automatically replace/update the running MystTiq application.
