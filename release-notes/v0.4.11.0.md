# v0.4.11.0 — Guilds + Bases Parity

- Restores guild search/status filters, CSV export, directory/detail panes, Copy Guild ID, leader-player navigation, and member/base evidence.
- Restores base search/status filters, CSV export, ownership evidence/detail, and Copy Base ID.
- Extends the existing headless explorer contract with authoritative decoded `base_ids`; the Avalonia client performs presentation only and never opens remote save paths.
- Preserves selection by stable guild/base ID across refreshes where possible.
- Keeps leader transfer, member repair, orphan claim, ownership repair, and recovery visibly disabled until preview and transactional mutation routes exist.
- Displays and tests the required Preview → Safety Backup → Server-side Transaction → Validate → Journal/Audit → Refresh GUI contract.

Card containment, centered metallic buttons, guarded close-before-relaunch, single aggregate periodic polling, Windows/Linux parity, and remote/LAN security remain preserved.

Promotion gate: strict validation reported 0 errors and 0 warnings; Windows and Linux headless/desktop builds succeeded; isolated runtime smoke passed; logic gate passed 170/170.
