# v0.4.16.0 — Activity & Audit + Notifications Parity

- Adds Activity/Audit search, severity/category filters and local visible-view export.
- Keeps persistent activity/audit evidence append-only from the GUI; clear remains policy-locked.
- Adds a bounded, atomic server-side Notification Center store.
- Restores notification badge/page, search/severity filters, read/unread, pin/unpin, dismiss, mark-all-read, semantic self-test and local export.
- Audits notification mutations with IDs/counts only, never message bodies.
- Preserves the one periodic status poll; Activity and Notifications refresh only on demand.

Promotion gate: strict validation reported 0 errors and 0 warnings; Windows/Linux headless and Avalonia builds succeeded; runtime notification persistence/mutation checks passed; logic gate passed 204/204. The updater repeats this complete gate on the installed tree.
