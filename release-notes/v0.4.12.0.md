# v0.4.12.0 — Backup Center Parity

- Restores create, refresh, delete, explicit-confirmation restore, Verify Selected, Verify All, retention preview/apply, and local Open Backup Root workflows.
- Deep verification reads every archive entry, rejects unsafe paths, records SHA-256 evidence, persists results atomically, and audits summaries without copying save content.
- Retention cleanup is server-side and remote-safe. Apply consumes a single-use expiring preview token, deletes exactly the previewed files, and requires re-preview when inventory changes.
- Restore preserves the existing stopped-server requirement, pre-restore safety backup, isolated staging, and rollback behavior.
- Local profiles may open the server backup folder; remote profiles remain on API-provided inventory and never open the same-looking local path.

Card containment, centered metallic buttons, close-before-relaunch, single aggregate polling, Windows/Linux Avalonia parity, and remote/LAN security remain preserved.

Promotion gate: strict validation reported 0 errors and 0 warnings; Windows and Linux headless/desktop builds succeeded; isolated runtime smoke passed; logic gate passed 176/176.
