# v0.4.17.3 — Crash Analyzer + Palworld Save Tools Parity

This fix carries the complete v0.4.17 feature set and corrects an installed-tree clean race discovered after the old desktop and sidecar had been stopped.

- Clean waits for verified artifact-hosted processes and retries only validated `artifacts`, `bin`, and `obj` directories while Windows releases DLL mappings.
- Deletion targets must resolve inside the project root and use an approved generated-directory leaf name.
- Crash analysis remains bounded/evidence-backed; Save Tools remains server-side and read-only.
- Single polling, Windows/Linux parity, secured remote/LAN operation, containment and centered metallic buttons remain preserved.

Promotion requires the complete source gate and updater-driven installed-tree repeat gate.
