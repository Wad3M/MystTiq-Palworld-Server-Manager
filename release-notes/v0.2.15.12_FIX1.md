# MystTiq Palworld Server Manager v0.2.15.12 FIX1

## Server Inspector Delegation Compile Hotfix

Windows compilation identified three stale helper calls in `ServerService.CleanupSessionAfterShutdownAsync` after process/session inspection was extracted.

FIX1 routes cleanup through `ServerSessionInspector` for:
- descendant process IDs
- guarded listening ports

It also corrects the remaining v0.2.15.11 source comment and strengthens the architecture harness.

The v0.2.15.12 modularization design and runtime behavior are otherwise unchanged.
