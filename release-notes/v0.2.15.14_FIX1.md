# MystTiq Palworld Server Manager v0.2.15.14 FIX1

## Composition Root Namespace Compile Hotfix

Windows compilation identified one missing namespace import in the new application composition root.

`ApplicationServiceComposition.cs` references `AppSettings`, which lives in `PalworldManager.Models`. FIX1 adds the required:

```csharp
using PalworldManager.Models;
```

No architecture or runtime behavior changes from v0.2.15.14.
