# v0.8.1.0 — Ribbon Icons

- **Your new Ribbon icons are in.** Refresh, Start, Restart, Stop, Backup, Console, Doctor, Verify Files, Install
  Missing and Force Stop now use your Palworld-inspired vector icons: capture spheres, the storage chest, the
  Palbox terminal, Pal ears, the shield, the workbench and the red stop seal.
- They are drawn as crisp vectors at 24px in the same theme colours as before, so they follow light/dark mode and
  each server tab's accent. Disabled buttons still dim as a whole.
- The other "Refresh …" buttons (Refresh Map, Refresh World, …) and "Restart Server" use the matching new icons too,
  so the Ribbon stays consistent. Every other button keeps its current symbol.
- Your icon package (SVGs, icons.json, preview, README) is kept with the source under `docs/ribbon-icons/`.
- Checked with 42 new rendering checks (every icon, every mapping, and the Ribbon drawn in dark and light), the full
  test gate, and the live app.
