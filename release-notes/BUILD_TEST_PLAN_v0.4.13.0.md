# v0.4.13.0 Build and Test Plan

Run Clean, strict Validate, the v0.4.13.0 logic harness with all Windows/Linux builds, and Windows isolated runtime smoke. Verify authoritative base IDs cross the headless DTO, search/filter/export/selection behavior, leader-player navigation, mutation capability truth, transaction-contract presentation, remote path safety, single polling, security, containment, and relaunch regression checks. Package only after the complete gate is clean.
