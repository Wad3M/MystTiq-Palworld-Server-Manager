# MystTiq Palworld Server Manager v0.3.1.3 FIX6

## Harness Assertion Cleanup

The v0.3.1.3 FIX5 deployment workflow passed in real use, including current-version archive selection and remote deployment, but the logic harness still had one false-negative source-text assertion.

FIX6 changes only the automated test harness:

- removes the brittle exact-line check for project-version resolution
- verifies the behavior contract instead:
  - empty `Version` detection is present
  - `Get-ProjectVersion.ps1` resolution is present
  - parser-safe Version parameter default remains present
  - current-version archive naming remains present

No deployment, monitoring, GUI, lifecycle, API, TLS, SSH, systemd, or PalServer runtime code changed.
