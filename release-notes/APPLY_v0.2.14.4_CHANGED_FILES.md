# Apply v0.2.14.4 Changed Files

Apply the changed-files archive over the validated v0.2.14.3 working tree, preserving directory structure. Then run `Build.ps1 Clean` followed by `Build.ps1 All`.

This update modifies World Inspector XAML/navigation, adds the World Management wizard integration and navigation handlers, updates the changelog, and adds release notes under `release-notes`.
