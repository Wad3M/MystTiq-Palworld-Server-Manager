# v0.7.65.0 Build and Test Plan

1. Close artifact-hosted MystTiq desktop and sidecar processes with Clean.
2. Run strict validation and the complete v0.7.65.0 logic suite (`scripts/Test-v0.7.65.0-Logic.ps1 -RunBuild`), including the frozen v0.7.64.0 checkpoint regression gate and the carried-forward v0.5.1.5/v0.7.12.0/v0.7.15.0/v0.7.17.0/v0.7.64.0 smoke suites.
3. Build shared, Windows/Linux headless, and Windows/Linux Avalonia targets. The Linux headless target (`Build.ps1 LinuxHeadless`) is the primary proof point this release — it requires the version-matched `Test-v0.7.65.0-LinuxAcceptance.sh`/`Test-v0.7.65.0-ProductionReadiness.sh` (carried forward from v0.7.62.0's copies this release) to exist to build at all.
4. New surface this release: `src/MystTiq.Core/Services/LinuxServerLifecycleService.cs` (`LaunchDetached` now targets `LogsRoot/MystTiq-PalServer-Console.log` instead of `ManagerRuntimeRoot/palserver-console.log`, calls `ConsoleLogRotation.RotateIfNeeded` once per server start), `scripts/Test-v0.7.65.0-LinuxAcceptance.sh` (new, one targeted check added), `scripts/Test-v0.7.65.0-ProductionReadiness.sh` (new, carried forward unchanged besides version string).
5. **Verified live against the real reference Linux VM** (correct address `192.168.1.143` — an earlier `.144` in this session's own notes was a stale typo, corrected by the user; the VM connected immediately once fixed). Rather than running the acceptance script's own `--extended` flag directly (it defaults to the live production config), built a fully isolated manual test: scratch directory under `mystroth`'s home, throwaway API port, non-conflicting game port, a stand-in `PalServer.sh` in place of the real binary. Confirmed live: `POST /api/v1/server/start` produces `LogsRoot/MystTiq-PalServer-Console.log` with the expected start marker and real captured stdout/stderr; padding that file past 25 MB and starting again correctly rotates it to a `.1` generation with content preserved and starts fresh. The real `mysttiq-palworld.service` was confirmed untouched (same PID) throughout, and all test artifacts were removed from the VM afterward.
6. Create FullSource and Changed Files ZIPs and verify their entries and SHA-256 hashes.

Any failure blocks promotion.
