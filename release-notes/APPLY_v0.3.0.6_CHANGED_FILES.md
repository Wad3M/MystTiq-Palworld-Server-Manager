# Apply Instructions — v0.3.0.6

Apply over official Linux/headless baseline **v0.3.0.5** while preserving repository-relative paths.

All operational helpers remain under `scripts`.

Run the normal Windows gate and passwordless Linux deployment acceptance before explicitly enrolling the remote API.

Remote enrollment is an opt-in test and must not be performed on an untrusted network.
