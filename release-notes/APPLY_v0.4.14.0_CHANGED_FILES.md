# Apply v0.4.14.0 Changed Files

Apply only to a verified v0.4.10.0 Full Source tree. Back up local changes, merge the archive at the repository root, then run the complete current-release gate. Prefer the Full Source package for unknown or older baselines.
