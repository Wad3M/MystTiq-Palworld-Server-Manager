# MystTiq v0.7.0.1 — Fix Release (Fleet Page Nav Sidebar Bug)

## Highlights

- **Fixed: the Fleet page's left nav sidebar was blank.** Opening Fleet made the entire System category's nav list disappear and left the System category tab looking unselected — the page still worked, but felt disconnected from the rest of the app. Fixed by including Fleet in its own category's page check.

## Upgrade notes

- No configuration schema change, no server-side change at all — this release is entirely `MystTiq.Desktop`.

Full detail in [`CHANGELOG.md`](../CHANGELOG.md).
