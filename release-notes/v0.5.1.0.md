# v0.5.1.0 — Functional Visual Shell Integration

v0.5.1.0 starts the functional v0.5 route from the clean v0.4.18.2 installed baseline. The supplied v0.4.17.4 package remains a comparison reference and is not used to downgrade the working tree.

- Uses the Palworld server icon with the supplied compact MystTiq wordmark and version beneath it.
- Keeps World navigation to Inspector, Players, Bases and Guilds.
- Keeps Settings, Activity & Audit and Notifications under System.
- Moves the transactional validator/recovery entry into Inspector so its functionality remains available without crowding World navigation.
- Routes the header bell to Notifications and gear to Settings.
- Adds a glass-highlight navigation hover.
- Preserves centered metallic buttons, global card containment, single aggregate status polling, secured LAN/remote profiles and Windows/Linux Avalonia builds.
- Preserves every operational View → ViewModel → API client → route → headless service chain from v0.4.18.2.

Promotion requires strict validation, the complete versioned logic suite, Windows/Linux headless and desktop builds, isolated API runtime smoke, visual launch verification, and clean package checksum verification.
