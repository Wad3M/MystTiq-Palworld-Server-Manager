# MystTiq Palworld Server Manager v0.3.1.2 FIX1

## Version Consistency Cleanup

The v0.3.1.2 GUI and lifecycle functionality passed acceptance, but release validation reported three stale-version warnings.

FIX1:

- removes the obsolete v0.3.1.1 cleanup script from active source
- changes the v0.3.1.2 cleanup helper so it does not embed the previous full version literally
- updates Avalonia desktop project metadata to v0.3.1.2
- adds regression checks for the cleanup/version-consistency rules

No GUI, lifecycle, API, TLS, connection-profile, headless-service, systemd, or PalServer behavior changed.
