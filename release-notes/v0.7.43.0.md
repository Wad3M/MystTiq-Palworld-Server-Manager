# MystTiq v0.7.43.0 — Findings Completeness Fixes

## Highlights

- **MOD Dashboard now has its own Installed MODs list + details panel** (read-only) — a gap from v0.7.40.0, which only applied this to MOD Library despite the original ask wanting it on Dashboard too.
- **The footer's "working on" indicator now shows elapsed time and which server** the operation belongs to.
- **Nav pane icons doubled again** (28×28 → 56×56), row height and icon column scaled to fit.

## Upgrade notes

- Desktop-only change, no configuration or server-side impact.
- Prompted by a direct request to verify earlier work actually shipped — both gaps found and fixed here.
- No live GUI verification in this environment.

Full detail in [`CHANGELOG.md`](../CHANGELOG.md).
