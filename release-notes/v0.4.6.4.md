# MystTiq v0.4.6.4 — Server Setup Parity, Navigation State & One-Command Update Gate

Official baseline remains **v0.4.5.1**. v0.4.6.4 is a development candidate and is not promotion-eligible until the complete Windows/Linux logic/build/runtime and manual GUI acceptance gate passes.

## Scope

- Rebuild Server Setup around the final v0.2.16.4 environment checklist and operation-monitor information architecture.
- Keep environment detection in the headless backend and expose it through `/api/v1/server/environment`.
- Provision SteamCMD when missing before Palworld Dedicated Server distribution operations.
- Keep the selected navigation destination visibly highlighted, including navigation triggered from another page.
- Source Dashboard Live Activity from console/log events and enrich the console with lifecycle startup evidence.
- Make `Update-FromDownloads.ps1` a true one-command source replacement + complete build/test gate.
- Continue the explicit page-by-page parity audit recorded in `docs/GUI_PARITY_v0.4.6.4.md`.

## Promotion blockers

Any failed validation, logic, compile, Windows/Linux package, runtime smoke, Server Setup acceptance, navigation-state acceptance, server lifecycle acceptance, or GUI parity regression blocks promotion.
