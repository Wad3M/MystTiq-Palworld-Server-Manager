# v0.4.15.0 Build and Test Plan

Run Clean, strict Validate, the v0.4.15.0 logic harness with all Windows/Linux builds, and Windows isolated runtime smoke. Verify validator/export wiring, side-effect-free Analyze, single-use expiry, confirmation/stopped-server gating, fresh backup, staging, atomic swap, post-validation, durable journal/audit, failure injection at every stage, rollback, traversal rejection, remote path safety, single polling, security, containment, and relaunch regressions. Package only after the complete gate is clean.
