# Apply Instructions — v0.3.0.4

Apply over official Linux/headless baseline **v0.3.0.3 FIX1** while preserving repository-relative paths.

The Changed Files package includes a deletion notice for the obsolete active `scripts\Test-v0.3.0.3-Logic.ps1` harness.

After application, use the v0.3.0.4 build/test plan. Utility/deployment/test scripts remain under the repository `scripts` directory.
