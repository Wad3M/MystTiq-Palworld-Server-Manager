# Apply Instructions — v0.4.0.1

Extract the Changed Files ZIP over `C:\GameServers\MystTiqPalLinux`, preserving folders and replacing matching files. Unblock PowerShell scripts, then run the v0.4.0.1 build/test plan.
