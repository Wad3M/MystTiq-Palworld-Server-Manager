# MystTiq Palworld Server Manager v0.3.1.7

## World Explorer Foundation

Added:

- authenticated `/api/v1/world/explorer`
- server-authoritative SaveRoot discovery
- Level.sav-based world candidate detection
- active-world selection by newest Level.sav evidence
- bounded active-world file inventory
- core world/player-save category projection
- shared Avalonia World Explorer page

World Explorer remains read-only in this phase.

## Warning cleanup

- Doctor version is assembly-derived rather than hard-coded.
- Secured/non-loopback extended lifecycle acceptance now reports `SKIP` rather than `WARN`.
- Linux acceptance summary adds a separate skipped count.
